import { chromium } from 'playwright'; import fs from 'node:fs/promises';
const out='images-agent-browser/theme-audit-20260711/final-smoke'; await fs.mkdir(out,{recursive:true});
const cases=[['home-desktop','https://www.zidooka.com/',1440,900,'light'],['post-desktop','https://www.zidooka.com/archives/4575',1440,900,'dark'],['post-mobile','https://www.zidooka.com/archives/4582',390,844,'dark']];
const b=await chromium.launch({headless:true});
for(const [name,url,w,h,scheme] of cases){const p=await b.newPage({viewport:{width:w,height:h},colorScheme:scheme});const errors=[];p.on('pageerror',e=>errors.push(String(e)));await p.goto(url+'?final_smoke='+Date.now(),{waitUntil:'domcontentloaded',timeout:30000});await p.waitForTimeout(1800);const m=await p.evaluate(()=>{const q=s=>{const e=document.querySelector(s);if(!e)return null;const r=e.getBoundingClientRect();return{width:Math.round(r.width),height:Math.round(r.height),x:Math.round(r.x),y:Math.round(r.y)}};return{clientWidth:document.documentElement.clientWidth,scrollWidth:document.documentElement.scrollWidth,bodyClass:document.body.className,campaign:q('.zdk-ad--campaign_top'),header:q('.zdk-site-header'),sidebarAd:q('.zdk-ad--sidebar')}});await p.screenshot({path:`${out}/${name}.png`,fullPage:false});console.log(name,JSON.stringify({...m,errors:errors.length}));await p.close()}await b.close();