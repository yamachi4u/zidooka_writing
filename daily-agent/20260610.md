# zidooka-writing エージェントログ 2026-06-10

## Opencode セッション — PostHog実験運用

### 実行内容

1. **Z1: TOC sticky本採用**
   - `single.css` に `.zenn-toc-wrapper { position: sticky; top: 1rem; ... }` 追加
   - `functions.php` から `.exp-toc-sticky` 実験CSS削除
   - `posthog-experiments.js` から `toc_sticky` フラグ読み取り＋適用ブロック削除
   - PostHog flag `zdk_toc_sticky` → inactive (API経由)
   - 結果: TOC Click +203%, Engaged 60s +17.3%

2. **Z2: 行間実験 (`zdk_line_height`) 開始**
   - PostHog API で flag active (control 50% / loose 50%)
   - 既存コードで動作 (JS/CSS実装済み)
   - 判定予定日: 2026-06-17
   - KPI: 読了深度・エンゲージメント

3. **Z3: TOC sticky本番デプロイ**
   - remote-agent push: single.css, functions.php, posthog-experiments.js
   - production JS確認済み (7639 bytes, forceInit含む)

4. **Z4: Null rate トラブルシュート**
   - 診断結果: 全15件の `zdk_flag_resolution_error` が `timeout` 型
   - `onFeatureFlags` は利用可だが10s以内に発火しないユーザーが存在
   - 対策: timeout 10s→15s + `forceInit()` fallback追加
   - 24-48hで改善見込み。次回 `posthog:check` で確認予定

### メモ
- Null rate問題は `forceInit()` fallbackで緩和予定。完全解決はPostHog SDK側の改善待ちかも
- 次回パイプライン: related_posts → code_fold → header_image
